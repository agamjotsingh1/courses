# Fixed point quantization error
## To run python implementation
```sh
python3 fixed_point.py
```

## To run verilog implementation
Plotting is done via python and data to plot is stored in `.csv` files in the `data/` directory.
```sh
chmod +x run_verilog_sim.sh
./run_verilog_sim.sh
```